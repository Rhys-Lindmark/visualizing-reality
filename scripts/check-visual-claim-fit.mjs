import fs from'node:fs';
const claims=JSON.parse(fs.readFileSync('public/data/claim-registry.json','utf8'));const audits=JSON.parse(fs.readFileSync('public/data/visual-claim-audit.json','utf8'));const index=new Map(claims.map(c=>[c.id,c]));const audited=new Set();const errors=[];for(const audit of audits){if(audited.has(audit.claim_id))errors.push(`${audit.claim_id}: duplicate audit`);audited.add(audit.claim_id);const claim=index.get(audit.claim_id);if(!claim)errors.push(`${audit.claim_id}: missing claim`);else if(claim.claim!==audit.claim)errors.push(`${audit.claim_id}: audit claim does not exactly match registry`);for(const field of['visual_question','visual_answer','default_visual','status'])if(!audit[field])errors.push(`${audit.claim_id}: missing ${field}`);if(audit.status!=='pass')errors.push(`${audit.claim_id}: visual has not passed claim-fit review`);}if(errors.length){console.error(errors.join('\n'));process.exit(1);}console.log(`Visual-claim fit passed: ${audits.length} audited argument(s).`);
