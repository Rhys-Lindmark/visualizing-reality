'use client';
import{useEffect,useMemo,useState}from'react';import{fetchClientText}from'./lib/clientAsset';
type Row={mechanism:string;period:string;military_problem:string;system_response:string;observable_anchor:string;evidence:string;source_keys:string;limits:string};const dataUrl='/data/steppe/20260901-war-system1/steppe-war-system.csv';
function parseCSV<T>(text:string):T[]{const rows:string[][]=[];let row:string[]=[],cell='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'&&quoted&&text[i+1]==='"'){cell+='"';i++;}else if(c==='"')quoted=!quoted;else if(c===','&&!quoted){row.push(cell);cell='';}else if((c==='\n'||c==='\r')&&!quoted){if(c==='\r'&&text[i+1]==='\n')i++;row.push(cell);if(row.some(Boolean))rows.push(row);row=[];cell='';}else cell+=c;}if(cell||row.length){row.push(cell);rows.push(row);}const[headers,...body]=rows;return body.map(values=>Object.fromEntries(headers.map((header,index)=>[header,values[index]??''])) as T);}
export default function SteppeWarSystem(){const[rows,setRows]=useState<Row[]>([]);const[state,setState]=useState<'loading'|'ready'|'error'>('loading');const[error,setError]=useState('');const[attempt,setAttempt]=useState(0);const[selectedMechanism,setSelectedMechanism]=useState('Weapon');useEffect(()=>{const controller=new AbortController();fetchClientText(dataUrl,{signal:controller.signal,label:'steppe warfare system evidence'}).then(text=>{const parsed=parseCSV<Row>(text);if(parsed.length!==4||parsed.some(row=>!row.mechanism||!row.system_response||!row.source_keys))throw new Error('The warfare evidence used an incompatible data contract.');setRows(parsed);setState('ready');}).catch(problem=>{if(controller.signal.aborted)return;setError(problem instanceof Error?problem.message:'The warfare evidence could not be loaded.');setState('error');});return()=>controller.abort();},[attempt]);const selected=useMemo(()=>rows.find(row=>row.mechanism===selectedMechanism)??rows[0],[rows,selectedMechanism]);if(state!=='ready'||!selected)return <div className={`data-state mongol-operations-state ${state}`} role={state==='error'?'alert':'status'}><b>{state==='error'?'The warfare evidence did not load':'Loading the steppe warfare system…'}</b><span>{state==='error'?error:'weapon · maneuver · remounts · information'}</span>{state==='error'&&<button type="button" onClick={()=>{setRows([]);setError('');setState('loading');setAttempt(value=>value+1);}}>Retry evidence</button>}</div>;return <div className="mongol-operations-chart"><nav aria-label="Choose a steppe warfare mechanism">{rows.map(row=><button type="button" key={row.mechanism} className={selected.mechanism===row.mechanism?'active':''} aria-pressed={selected.mechanism===row.mechanism} onClick={()=>setSelectedMechanism(row.mechanism)}><span>{row.period}</span><b>{row.mechanism}</b></button>)}</nav><section aria-live="polite"><header><div><span>System response</span><h5>{selected.system_response}</h5></div><strong>{selected.observable_anchor}</strong></header><div className="mongol-operations-lanes"><article><span>Problem</span><p>{selected.military_problem}</p></article><article><span>Evidence</span><p>{selected.evidence}</p></article><article><span>Limit</span><p>{selected.limits}</p></article></div></section></div>}
