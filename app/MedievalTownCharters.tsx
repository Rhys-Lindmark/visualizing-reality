'use client';
import{useEffect,useMemo,useState}from'react';import{fetchClientText}from'./lib/clientAsset';
type Row={case:string;date_label:string;bargain:string;collective_right:string;how_it_endured:string;evidence:string;visual_anchor:string;source_keys:string;limits:string};const dataUrl='/data/medieval-europe/20260831-charters1/town-charter-rights.csv';
function parseCSV<T>(text:string):T[]{const rows:string[][]=[];let row:string[]=[],cell='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'&&quoted&&text[i+1]==='"'){cell+='"';i++;}else if(c==='"')quoted=!quoted;else if(c===','&&!quoted){row.push(cell);cell='';}else if((c==='\n'||c==='\r')&&!quoted){if(c==='\r'&&text[i+1]==='\n')i++;row.push(cell);if(row.some(Boolean))rows.push(row);row=[];cell='';}else cell+=c;}if(cell||row.length){row.push(cell);rows.push(row);}const[headers,...body]=rows;return body.map(values=>Object.fromEntries(headers.map((header,index)=>[header,values[index]??''])) as T);}
export default function MedievalTownCharters(){const[rows,setRows]=useState<Row[]>([]);const[state,setState]=useState<'loading'|'ready'|'error'>('loading');const[error,setError]=useState('');const[attempt,setAttempt]=useState(0);const[selectedCase,setSelectedCase]=useState('Freiburg');useEffect(()=>{const controller=new AbortController();fetchClientText(dataUrl,{signal:controller.signal,label:'Medieval town charters'}).then(text=>{const parsed=parseCSV<Row>(text);if(parsed.length!==4||parsed.some(row=>!row.case||!row.collective_right||!row.source_keys))throw new Error('The charter evidence used an incompatible data contract.');setRows(parsed);setState('ready');}).catch(problem=>{if(controller.signal.aborted)return;setError(problem instanceof Error?problem.message:'The evidence could not be loaded.');setState('error');});return()=>controller.abort();},[attempt]);const selected=useMemo(()=>rows.find(row=>row.case===selectedCase)??rows[0],[rows,selectedCase]);if(state!=='ready'||!selected)return <div className={`data-state medieval-charters-state ${state}`} role={state==='error'?'alert':'status'}><b>{state==='error'?'The charter evidence did not load':'Loading charter bargains…'}</b><span>{state==='error'?error:'freedom · dues · trade · portable rights'}</span>{state==='error'&&<button type="button" onClick={()=>{setRows([]);setError('');setState('loading');setAttempt(value=>value+1);}}>Retry evidence</button>}</div>;return <div className="medieval-charters-chart"><nav aria-label="Choose a charter case">{rows.map(row=><button type="button" key={row.case} className={selected.case===row.case?'active':''} aria-pressed={selected.case===row.case} onClick={()=>setSelectedCase(row.case)}><span>{row.date_label}</span><b>{row.case}</b></button>)}</nav><section aria-live="polite"><header><div><span>The bargain</span><h5>{selected.bargain}</h5></div><strong>{selected.visual_anchor}</strong></header><div className="medieval-charters-lanes"><article><span>Collective right</span><p>{selected.collective_right}</p></article><article><span>How it endured</span><p>{selected.how_it_endured}</p></article><article><span>Evidence</span><p>{selected.evidence}</p></article></div></section></div>}
