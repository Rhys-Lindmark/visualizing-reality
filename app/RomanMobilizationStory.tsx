'use client';
import { useState } from 'react';
import RomanCapacityReconstruction from './RomanCapacityReconstruction';
import RomanAllianceComparison from './RomanAllianceComparison';
import ChartFooter from './components/ChartFooter';

export default function RomanMobilizationStory(){const[view,setView]=useState<'argument'|'estimates'>('argument');return <div className="roman-mobilization-story"><nav className="roman-story-tabs" aria-label="Choose the Rome military view"><button type="button" className={view==='argument'?'active':''} onClick={()=>setView('argument')}>Why Rome won</button><button type="button" className={view==='estimates'?'active':''} onClick={()=>setView('estimates')}>Continuous estimate</button></nav>{view==='argument'?<RomanAllianceComparison/>:<div className="viz-card manpower-viz-v2"><div className="viz-title"><div><h4>Rome sustained unusually large armies for centuries</h4><p>A transparent annual reconstruction turns seven published anchors into one working estimate.</p></div></div><RomanCapacityReconstruction/><ChartFooter source="Ancient force reports and modern reconstructions by Brunt, Taylor, Campbell, Hassall, MacMullen, Elton, and Treadgold" note="Annual values are linear interpolation between seven unlike but explicit Roman anchors. This is a working reconstruction, not annual observation; use the dots and method note to judge it." dataHref="/data/rome/20260830-client4/roman-military-capacity.csv"/></div>}</div>}
